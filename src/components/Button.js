

export const Button = ({ children } ) => {
  return (
     <button className = "w-64 text-xl font-medium text-white bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 ">{ children }</button>
  )
}
